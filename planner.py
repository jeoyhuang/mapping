# theorectically map is made from the json file
# pos should just be an (x,y) of current position
# tasks include pick up location and deliver location, and priority (enum/int)
# example task: {"pickup" : [x1,y1], "deliver" : [x2,y2], "priority" : int}
def gen_waypoints(map, pos, tasks):
  waypoints = []

  # pick out the tasks and list them as waypoints based on algorithm
  # add to waypoints list

  return waypoints